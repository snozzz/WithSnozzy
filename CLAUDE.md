# WithSnozzy

macOS 上的 lofi 陪伴应用，作者自用。女主叫 Snozzy。

**动手前先读 `HANDOFF.md`** —— 里面有美术管线、十四条踩过的坑、
当前状态和待办。那些坑全是"代码看着对但画面错"的类型，重复犯很浪费时间。

## 硬约束

- 包体 < 1GB、运行内存 < 500MB。当前包 11MB、闲时约 66MB
- 零第三方 Swift 依赖。`Package.swift` + `Scripts/build_app.sh` 就是全部构建系统
- 代码优化到极致，**同时**要方便维护
- 每完成一个功能就 push 到 `git@github.com:snozzz/WithSnozzy.git`
- **提交描述简约克制**：说清楚改了什么、为什么，不要煽情不要堆形容词

## 工作方式

把主观的东西做成可测的。这个项目里"看着对"和"真的对"经常不是一回事：

- 角色出没出画 → `snozzy_lib.frame_extent()` 报数值
- 重绘漂没漂 → 剪影交并比
- UI 行为对不对 → 截两张图算像素差

盯着截图猜原因至少浪费过两轮，先量一下往往就省了。
